info:
  name: getUpdates
  type: http
  seq: 2

http:
  method: GET
  url: https://api.telegram.org/bot<TOKEN>/getUpdates
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
